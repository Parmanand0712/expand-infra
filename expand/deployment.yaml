apiVersion: apps/v1
kind: Deployment
metadata:
  name: expand
spec:
  replicas: 2
  revisionHistoryLimit: 2
  selector:
    matchLabels:
      app: expand
  template:
    metadata:
      labels:
        app: expand
    spec:
      containers:
      - image: parmanand0712/expand:74df88c
        name: expand
        ports:
        - containerPort: 3000
